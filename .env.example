# Exemplo de variáveis de ambiente
# Defina um valor para API_KEY e sua string de conexão do banco

API_KEY=valor
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Porta do servidor (opcional)
PORT=3001
NODE_ENV=development
