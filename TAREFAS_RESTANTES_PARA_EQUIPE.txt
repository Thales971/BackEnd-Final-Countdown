TAREFAS RESTANTES PARA EQUIPE - BackEnd-Final-Countdown
=====================================================

Resumo
------
Este documento divide as tarefas restantes para concluir a atividade do backend (Produto / Fornecedor) em 6 responsabilidades, fornece instruções detalhadas e inclui todos os códigos que faltam (controllers, routes, server, PDF util, exemplos Postman, e instruções para UML/DER). Use este arquivo como um "manual de conclusão".

Regras importantes
------------------
- NÃO altere `prisma/schema.prisma` ou `prisma/seed.js`.
- Use os nomes e rotas exatos exigidos pela atividade:
  - Fornecedor (Entidade Principal): `/principal` (POST, GET, GET/:id, PUT/:id, DELETE/:id)
  - Produto (Entidade Catálogo): `/catalogo` (POST, GET, GET/:id, PUT/:id, DELETE/:id)
  - Foto Produto: `POST /catalogo/:id/foto` e `GET /catalogo/:id/foto`
  - PDF: `GET /catalogo/:id/pdf` e `GET /catalogo/pdf`
- Autenticação: todas as rotas `/principal` exigem header `X-API-Key` com valor `API_KEY` no `.env`.
- Mensagens de erro EXATAS (usar texto):
  - "Campo obrigatório não informado."
  - "Registro não encontrado."
  - "CEP inválido."
  - "CEP não encontrado."
  - "Serviço externo indisponível."
  - "Item indisponível."
  - "Operação não permitida para registro inativo."
  - "Acesso negado."
  - "Categoria inválida."
  - "Não é permitido utilizar item indisponível."

Como rodar (pré-requisitos)
---------------------------
1. Ter Node.js (>=18 recomendado).
2. Criar `.env` baseado em `.env.example` com `API_KEY` e `DATABASE_URL`.
3. Instalar dependências: `npm install`.
4. Gerar prisma (se necessário): `npm run prisma:generate`.
5. Rodar seed (se tiver BD local acessível): `npm run seed`.
6. Rodar servidor em dev: `npm run dev`.

Arquivos e funcionalidades que faltam (visão geral)
--------------------------------------------------
- Renomear/ajustar models: mover `src/models/FornecedorService.js` → `src/models/FornecedorModel.js` e `src/models/ProdutoService.js` → `src/models/ProdutoModel.js` (mantendo o conteúdo/validações que já existem).
- Criar controllers: `src/controllers/fornecedorController.js` e `src/controllers/produtoController.js`.
- Criar rotas: `src/routes/principal.routes.js` e `src/routes/catalogo.routes.js`.
- Atualizar `src/server.js` para montar rotas sem o prefixo `/api`, servir `uploads/` e registrar middlewares (apiKey para `/principal`).
- Criar util `src/utils/pdf.js` para gerar relatórios individuais e geral com `html-pdf-node` (foto em base64).
- Ajustes finais: garantir que upload com Multer+Sharp funcione e substitua imagens; garantir regras de negócio (bloquear operações quando `ativo=false`, `disponivel=false`, validações de CEP/categoria/preço), testes via Postman, UML/DER+screenshots.

Divisão das tarefas por pessoa (6 integrantes)
-----------------------------------------------
Objetivo: cada integrante fica responsável por conjunto de arquivos pequenos e testáveis. Após concluir, abrir PR/commit e marcar o responsável pela revisão.

Tarefas atribuídas:

1) Thales (coordenador) — Integração principal, revisão geral
- Renomear os services para Models:
  - `src/models/FornecedorModel.js` (criar usando conteúdo de `FornecedorService.js`) 
  - `src/models/ProdutoModel.js` (criar usando conteúdo de `ProdutoService.js`)
- Criar `src/controllers/fornecedorController.js` (código abaixo)
- Criar `src/routes/principal.routes.js` (código abaixo)
- Atualizar `src/server.js` (código abaixo)
- Validar rotas `/principal` com `X-API-Key`
- Revisar PRs dos demais

2) Jennifer — Produtos: controllers, rotas, upload
- Criar `src/controllers/produtoController.js` (código abaixo)
- Criar `src/routes/catalogo.routes.js` (código abaixo)
- Implementar o endpoint `POST /catalogo/:id/foto` usando `src/utils/upload.js` (já criado)
- Testar cenários de negócio (categoria inválida, preço negativo, disponivel=false)

3) Letícia — PDF e templates
- Criar `src/utils/pdf.js` (código abaixo)
- Implementar templates HTML para relatório individual e geral
- Testar `GET /catalogo/:id/pdf` e `GET /catalogo/pdf` (validar PDF binário)

4) Isabela Duetes — Infra / Static / upload folder
- Garantir pasta `uploads/` presente e permissões corretas
- Ajustar servidor para servir arquivos estáticos `GET /catalogo/:id/foto` (ou `GET /uploads/:file`)
- Verificar substituição automática de imagem anterior

5) Luana — Documentação & UML & DER
- Criar `README.md` com instruções de uso e endpoints (base neste tutorial)
- Gerar UML de classes (pode usar draw.io, plantuml, ou mermaid) e exportar PDF
- Abrir Prisma Studio e exportar DER/PDF (screenshots)

6) Jhonatan — Testes, Postman e entrega final
- Criar Postman collection com todas as rotas e um ambiente (baseUrl, API_KEY)
- Gerar prints/screenshots dos endpoints funcionando (incluindo upload e PDF)
- Fazer testes de ponta-a-ponta e documentar erros/ajustes
- Montar checklist final e publicar no GitHub (README + links)

---

Abaixo estão os códigos sugeridos PARA COPIAR/COLE diretamente (cada pessoa pega os blocos referentes a sua tarefa).

==== 1) Renomear models (Thales) ====

Observação: você já tem `src/models/FornecedorService.js` e `src/models/ProdutoService.js`. Para seguir o padrão "Model" crie novos arquivos com os nomes abaixo (copie o conteúdo) e delete/renomeie os antigos.

Arquivo: src/models/FornecedorModel.js
-------------------------------------
```js
import prisma from '../utils/prismaClient.js';
import { consultarCEP } from '../utils/viaCep.js';

class FornecedorModel {
  static async criar(payload) {
    const { nome, email, telefone, cnpj, cep, ativo = true } = payload;

    if (!nome || typeof nome !== 'string' || nome.trim().length < 3 || nome.trim().length > 100) {
      throw new Error('Campo obrigatório não informado.');
    }

    if (!cep) {
      throw new Error('Campo obrigatório não informado.');
    }

    const endereco = await consultarCEP(String(cep));

    const data = {
      nome: nome.trim(),
      email: email || null,
      telefone: telefone || null,
      cnpj: cnpj || null,
      cep: endereco.cep,
      logradouro: endereco.logradouro,
      bairro: endereco.bairro,
      localidade: endereco.localidade,
      uf: endereco.uf,
      ativo: ativo === undefined ? true : Boolean(ativo),
    };

    return prisma.fornecedor.create({ data });
  }

  static async buscarTodos(filtros = {}) {
    const where = {};

    if (filtros.nome) where.nome = { contains: String(filtros.nome), mode: 'insensitive' };
    if (filtros.email) where.email = { contains: String(filtros.email), mode: 'insensitive' };
    if (filtros.localidade) where.localidade = { contains: String(filtros.localidade), mode: 'insensitive' };

    return prisma.fornecedor.findMany({ where });
  }

  static async buscarPorId(id) {
    const intId = parseInt(id, 10);
    if (Number.isNaN(intId)) return null;
    const data = await prisma.fornecedor.findUnique({ where: { id: intId } });
    return data || null;
  }

  static async atualizar(id, payload) {
    const intId = parseInt(id, 10);
    if (Number.isNaN(intId)) throw new Error('Registro não encontrado.');

    const existente = await prisma.fornecedor.findUnique({ where: { id: intId } });
    if (!existente) throw new Error('Registro não encontrado.');
    if (existente.ativo === false) throw new Error('Operação não permitida para registro inativo.');

    const { nome, email, telefone, cnpj, cep, ativo } = payload;

    if (nome !== undefined) {
      if (!nome || typeof nome !== 'string' || nome.trim().length < 3 || nome.trim().length > 100) {
        throw new Error('Campo obrigatório não informado.');
      }
    }

    if (!cep) throw new Error('Campo obrigatório não informado.');

    const endereco = await consultarCEP(String(cep));

    const data = {
      nome: nome !== undefined ? nome.trim() : existente.nome,
      email: email !== undefined ? email : existente.email,
      telefone: telefone !== undefined ? telefone : existente.telefone,
      cnpj: cnpj !== undefined ? cnpj : existente.cnpj,
      cep: endereco.cep,
      logradouro: endereco.logradouro,
      bairro: endereco.bairro,
      localidade: endereco.localidade,
      uf: endereco.uf,
      ativo: ativo !== undefined ? Boolean(ativo) : existente.ativo,
    };

    return prisma.fornecedor.update({ where: { id: intId }, data });
  }

  static async deletar(id) {
    const intId = parseInt(id, 10);
    if (Number.isNaN(intId)) throw new Error('Registro não encontrado.');

    const existente = await prisma.fornecedor.findUnique({ where: { id: intId } });
    if (!existente) throw new Error('Registro não encontrado.');
    if (existente.ativo === false) throw new Error('Operação não permitida para registro inativo.');

    return prisma.fornecedor.delete({ where: { id: intId } });
  }
}

export default FornecedorModel;
```

Arquivo: src/models/ProdutoModel.js
----------------------------------
```js
import prisma from '../utils/prismaClient.js';

const CATEGORIAS = ['ELETRONICOS', 'VESTUARIO', 'ALIMENTOS', 'MOVEIS'];

class ProdutoModel {
  static async criar(payload) {
    const { nome, descricao, categoria, disponivel = true, preco, fornecedorId } = payload;

    if (!nome || typeof nome !== 'string' || nome.trim().length === 0) {
      throw new Error('Campo obrigatório não informado.');
    }

    if (!categoria || !CATEGORIAS.includes(categoria)) {
      throw new Error('Categoria inválida.');
    }

    if (preco === undefined || preco === null || Number(preco) < 0) {
      throw new Error('Campo obrigatório não informado.');
    }

    const data = {
      nome: nome.trim(),
      descricao: descricao || null,
      categoria,
      disponivel: Boolean(disponivel),
      preco: Number(preco),
      foto: null,
      fornecedorId: fornecedorId || null,
    };

    return prisma.produto.create({ data });
  }

  static async buscarTodos(filtros = {}) {
    const where = {};

    if (filtros.nome) where.nome = { contains: String(filtros.nome), mode: 'insensitive' };
    if (filtros.categoria) where.categoria = filtros.categoria;
    if (filtros.disponivel !== undefined) where.disponivel = filtros.disponivel === 'true' || filtros.disponivel === true;

    return prisma.produto.findMany({ where });
  }

  static async buscarPorId(id) {
    const intId = parseInt(id, 10);
    if (Number.isNaN(intId)) return null;
    const data = await prisma.produto.findUnique({ where: { id: intId } });
    return data || null;
  }

  static async atualizar(id, payload) {
    const intId = parseInt(id, 10);
    if (Number.isNaN(intId)) throw new Error('Registro não encontrado.');

    const existente = await prisma.produto.findUnique({ where: { id: intId } });
    if (!existente) throw new Error('Registro não encontrado.');

    if (existente.disponivel === false) {
      // se produto está indisponível, bloquear operações de uso
      throw new Error('Não é permitido utilizar item indisponível.');
    }

    const { nome, descricao, categoria, disponivel, preco, fornecedorId, foto } = payload;

    if (categoria !== undefined && !CATEGORIAS.includes(categoria)) throw new Error('Categoria inválida.');
    if (preco !== undefined && Number(preco) < 0) throw new Error('Campo obrigatório não informado.');

    // se marcando como disponível e não existe foto, falhar (requisito: foto obrigatória após cadastro)
    if ((disponivel === true || (disponivel === undefined && existente.disponivel === true)) && (!existente.foto && !foto)) {
      throw new Error('Campo obrigatório não informado.');
    }

    const data = {
      nome: nome !== undefined ? String(nome).trim() : existente.nome,
      descricao: descricao !== undefined ? descricao : existente.descricao,
      categoria: categoria !== undefined ? categoria : existente.categoria,
      disponivel: disponivel !== undefined ? Boolean(disponivel) : existente.disponivel,
      preco: preco !== undefined ? Number(preco) : existente.preco,
      fornecedorId: fornecedorId !== undefined ? fornecedorId : existente.fornecedorId,
      foto: foto !== undefined ? foto : existente.foto,
    };

    return prisma.produto.update({ where: { id: intId }, data });
  }

  static async deletar(id) {
    const intId = parseInt(id, 10);
    if (Number.isNaN(intId)) throw new Error('Registro não encontrado.');

    const existente = await prisma.produto.findUnique({ where: { id: intId } });
    if (!existente) throw new Error('Registro não encontrado.');

    if (existente.disponivel === false) throw new Error('Não é permitido utilizar item indisponível.');

    return prisma.produto.delete({ where: { id: intId } });
  }
}

export default ProdutoModel;
```

==== 2) Controllers (Thales + Jennifer) ====

Arquivo: src/controllers/fornecedorController.js
------------------------------------------------
```js
import FornecedorModel from '../models/FornecedorModel.js';

export const criar = async (req, res) => {
  try {
    const data = await FornecedorModel.criar(req.body);
    return res.status(201).json(data);
  } catch (err) {
    const msg = err.message;
    if (['Campo obrigatório não informado.', 'CEP inválido.', 'CEP não encontrado.', 'Serviço externo indisponível.'].includes(msg)) {
      return res.status(400).json({ error: msg });
    }
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const listar = async (req, res) => {
  try {
    const registros = await FornecedorModel.buscarTodos(req.query);
    return res.status(200).json(registros);
  } catch (err) {
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const obter = async (req, res) => {
  try {
    const { id } = req.params;
    const registro = await FornecedorModel.buscarPorId(id);
    if (!registro) return res.status(404).json({ error: 'Registro não encontrado.' });
    return res.status(200).json(registro);
  } catch (err) {
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const atualizar = async (req, res) => {
  try {
    const { id } = req.params;
    const data = await FornecedorModel.atualizar(id, req.body);
    return res.status(200).json(data);
  } catch (err) {
    const msg = err.message;
    if (['Campo obrigatório não informado.', 'CEP inválido.', 'CEP não encontrado.', 'Serviço externo indisponível.', 'Operação não permitida para registro inativo.'].includes(msg)) {
      return res.status(400).json({ error: msg });
    }
    if (msg === 'Registro não encontrado.') return res.status(404).json({ error: msg });
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const deletar = async (req, res) => {
  try {
    const { id } = req.params;
    await FornecedorModel.deletar(id);
    return res.status(200).json({ message: 'Registro deletado com sucesso.' });
  } catch (err) {
    const msg = err.message;
    if (['Registro não encontrado.'].includes(msg)) return res.status(404).json({ error: msg });
    if (['Operação não permitida para registro inativo.'].includes(msg)) return res.status(400).json({ error: msg });
    return res.status(500).json({ error: 'Erro interno.' });
  }
};
```

Arquivo: src/controllers/produtoController.js
--------------------------------------------
```js
import ProdutoModel from '../models/ProdutoModel.js';
import { upload, saveImage, generateFileName } from '../utils/upload.js';

// Obs: o middleware de upload será aplicado nas rotas com upload.upload.single('foto')

export const criar = async (req, res) => {
  try {
    const data = await ProdutoModel.criar(req.body);
    return res.status(201).json(data);
  } catch (err) {
    const msg = err.message;
    if (['Campo obrigatório não informado.', 'Categoria inválida.'].includes(msg)) {
      return res.status(400).json({ error: msg });
    }
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const listar = async (req, res) => {
  try {
    const registros = await ProdutoModel.buscarTodos(req.query);
    return res.status(200).json(registros);
  } catch (err) {
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const obter = async (req, res) => {
  try {
    const { id } = req.params;
    const registro = await ProdutoModel.buscarPorId(id);
    if (!registro) return res.status(404).json({ error: 'Registro não encontrado.' });
    return res.status(200).json(registro);
  } catch (err) {
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const atualizar = async (req, res) => {
  try {
    const { id } = req.params;
    const data = await ProdutoModel.atualizar(id, req.body);
    return res.status(200).json(data);
  } catch (err) {
    const msg = err.message;
    if (['Campo obrigatório não informado.', 'Categoria inválida.', 'Não é permitido utilizar item indisponível.'].includes(msg)) {
      return res.status(400).json({ error: msg });
    }
    if (msg === 'Registro não encontrado.') return res.status(404).json({ error: msg });
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

// Upload de foto: usar middleware multer (upload.single('foto')) antes
export const uploadFoto = async (req, res) => {
  try {
    if (!req.file) return res.status(400).json({ error: 'Campo obrigatório não informado.' });

    const { id } = req.params;
    const produto = await ProdutoModel.buscarPorId(id);
    if (!produto) return res.status(404).json({ error: 'Registro não encontrado.' });

    // gerar nome e salvar
    const filename = generateFileName();
    const relativePath = await saveImage(req.file.buffer, filename, produto.foto);

    // atualizar produto com caminho da foto
    const atualizado = await ProdutoModel.atualizar(id, { foto: relativePath });
    return res.status(200).json(atualizado);
  } catch (err) {
    const msg = err.message;
    if (['Registro não encontrado.'].includes(msg)) return res.status(404).json({ error: msg });
    return res.status(500).json({ error: 'Erro interno.' });
  }
};

export const pegarFoto = async (req, res) => {
  try {
    const { id } = req.params;
    const produto = await ProdutoModel.buscarPorId(id);
    if (!produto) return res.status(404).json({ error: 'Registro não encontrado.' });
    if (!produto.foto) return res.status(404).json({ error: 'Registro não encontrado.' });

    // o servidor estará servindo a pasta uploads como estático em /uploads
    return res.status(200).json({ foto: produto.foto });
  } catch (err) {
    return res.status(500).json({ error: 'Erro interno.' });
  }
};
```

==== 3) Rotas (Thales + Jennifer) ====

Arquivo: src/routes/principal.routes.js
--------------------------------------
```js
import express from 'express';
import * as fornecedorCtrl from '../controllers/fornecedorController.js';
import apiKey from '../utils/apiKey.js';

const router = express.Router();

// todas as rotas exigem API KEY
router.post('/', apiKey, fornecedorCtrl.criar);
router.get('/', apiKey, fornecedorCtrl.listar);
router.get('/:id', apiKey, fornecedorCtrl.obter);
router.put('/:id', apiKey, fornecedorCtrl.atualizar);
router.delete('/:id', apiKey, fornecedorCtrl.deletar);

export default router;
```

Arquivo: src/routes/catalogo.routes.js
-------------------------------------
```js
import express from 'express';
import * as produtoCtrl from '../controllers/produtoController.js';
import { upload } from '../utils/upload.js';

const router = express.Router();

router.post('/', produtoCtrl.criar);
router.get('/', produtoCtrl.listar);
router.get('/:id', produtoCtrl.obter);
router.put('/:id', produtoCtrl.atualizar);
router.delete('/:id', produtoCtrl.deletar);

// Foto
router.post('/:id/foto', upload.single('foto'), produtoCtrl.uploadFoto);
router.get('/:id/foto', produtoCtrl.pegarFoto);

// PDF endpoints (Letícia implementa funções de geração)
router.get('/:id/pdf', produtoCtrl.pdfIndividual);
router.get('/pdf', produtoCtrl.pdfGeral);

export default router;
```

Nota: `produtoCtrl.pdfIndividual` e `produtoCtrl.pdfGeral` serão funções que delegam para `src/utils/pdf.js` (feito por Letícia). Se preferir, Jennifer pode implementar os endpoints chamando diretamente o util depois.

==== 4) Atualizar server.js (Thales) ====

Substituir o conteúdo de `src/server.js` pelo código abaixo:

```js
import express from 'express';
import 'dotenv/config';
import cors from 'cors';
import path from 'path';
import principalRoutes from './routes/principal.routes.js';
import catalogoRoutes from './routes/catalogo.routes.js';

const app = express();
app.use(cors());
app.use(express.json());

// servir imagens salvas
app.use('/uploads', express.static(path.join(process.cwd(), 'uploads')));

// rotas sem prefixo /api — conforme atividade
app.use('/principal', principalRoutes);
app.use('/catalogo', catalogoRoutes);

app.get('/', (req, res) => res.send('API funcionando'));

app.use((req, res) => {
  res.status(404).json({ error: 'Rota não encontrada' });
});

app.use((err, req, res, next) => {
  console.error(err);
  res.status(500).json({ error: 'Erro interno.' });
});

const PORT = process.env.PORT || 3001;
app.listen(PORT, () => console.log(`Servidor rodando em http://localhost:${PORT}`));
```

==== 5) PDF util (Letícia) ====

Arquivo: src/utils/pdf.js
-------------------------
```js
import pdf from 'html-pdf-node';

function produtoParaHtml(produto, fotoBase64 = null) {
  return `
    <html>
      <body>
        <h1>Produto: ${produto.nome}</h1>
        ${fotoBase64 ? `<img src="data:image/jpeg;base64,${fotoBase64}" style="max-width:300px;" />` : ''}
        <ul>
          <li>Descrição: ${produto.descricao || ''}</li>
          <li>Categoria: ${produto.categoria}</li>
          <li>Preço: ${produto.preco}</li>
          <li>Disponível: ${produto.disponivel}</li>
        </ul>
      </body>
    </html>
  `;
}

export async function gerarPdfBuffer(html) {
  const file = { content: html };
  const options = { format: 'A4' };
  const pdfBuffer = await pdf.generatePdf(file, options);
  return pdfBuffer;
}

export async function gerarPdfIndividual(produto, fotoBase64 = null) {
  const html = produtoParaHtml(produto, fotoBase64);
  return gerarPdfBuffer(html);
}

export async function gerarPdfGeral(produtos) {
  const rows = produtos.map(p => `<tr><td>${p.id}</td><td>${p.nome}</td><td>${p.categoria}</td><td>${p.preco}</td></tr>`).join('');
  const html = `<html><body><h1>Catálogo</h1><table border="1"><thead><tr><th>ID</th><th>Nome</th><th>Categoria</th><th>Preço</th></tr></thead><tbody>${rows}</tbody></table></body></html>`;
  return gerarPdfBuffer(html);
}

export default { gerarPdfIndividual, gerarPdfGeral };
```

Instrução: nos controllers, para o relatório individual, ler o `produto.foto` do DB (é um caminho `uploads/xxx.jpg`), converter para base64 (fs.readFile) e passar para `gerarPdfIndividual`.

==== 6) Notas Postman e testes (Jhonatan) ====

- Criar um ambiente com variáveis:
  - `baseUrl = http://localhost:3001`
  - `API_KEY = valor` (o mesmo do `.env`)
- Requests a adicionar:
  - POST {{baseUrl}}/principal (HEADER: X-API-Key: {{API_KEY}}) — body JSON com `nome` e `cep` obrigatório
  - GET {{baseUrl}}/principal
  - POST {{baseUrl}}/catalogo — criar produto (sem foto)
  - POST {{baseUrl}}/catalogo/:id/foto — enviar form-data `foto` (file) → testar substituição
  - GET {{baseUrl}}/catalogo/:id/pdf → baixar PDF
  - GET {{baseUrl}}/catalogo/pdf → baixar PDF geral

Gerar collection exportada e salvar em `postman_collection.json` no repositório.

==== 7) UML / DER / Entregáveis (Luana) ====

- UML de classes (pelo menos Class Diagram): Fornecedor e Produto com atributos do prisma/schema.prisma.
- DER: abrir Prisma Studio (`npm run prisma:studio`), tirar screenshot/print do modelo e salvar como PDF.
- README.md: como executar, endpoints, variáveis de ambiente, checklist final.

Checklist final antes de PR
---------------------------
- [ ] `npm run seed` inseriu 5 fornecedores e 5 produtos (conforme seed)
- [ ] Rotas `/principal` protegidas por `X-API-Key` e retornam 401 "Acesso negado." quando ausente
- [ ] POST/PUT `/principal` validam CEP via ViaCEP com mensagens de erro exatas
- [ ] Upload de foto redimensiona para 800px máximo, converte para JPEG qualidade 80 e substitui arquivo anterior
- [ ] PDFs gerados (individual e geral), foto embutida em base64 no individual
- [ ] Mensagens de erro exatas usadas em todos os lugares onde aplicável
- [ ] README, UML PDF, DER PDF, Postman collection incluídos no repositório

Dicas rápidas de desenvolvimento
--------------------------------
- Sempre rodar `npm run dev` e testar endpoints via Postman.
- Se `fetch` em `viaCep.js` falhar no seu Node, instale `node-fetch` e ajuste: `import fetch from 'node-fetch';`.
- Para debugging, inspecione `uploads/` para garantir que as imagens estão sendo salvas.

---

Se quiser, eu posso:
- Gerar os controllers/rotas automaticamente (já tenho o código acima pronto).
- Renomear os arquivos `FornecedorService.js` e `ProdutoService.js` para `FornecedorModel.js` e `ProdutoModel.js` automaticamente.
- Implementar os endpoints de PDF dentro dos controllers (Jennifer/Letícia podem revisar).

Diga qual opção prefere agora: eu crio/renomeio os arquivos automaticamente (aplico patches) ou a equipe prefere seguir o tutorial manualmente? 

Boa sorte — com isso a equipe deverá cobrir todos os requisitos da atividade. Quando quiser, eu aplico os patches automaticamente e testo localmente (dentro das limitações do ambiente).